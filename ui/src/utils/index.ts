import { getCookie, setCookie, hasCookie, removeCookie } from "@/utils/cookies";
import {
  getFromLocalStorege,
  saveInLocalStoreg,
  isInLocalStorge,
  deleteFromLocalStorge,
} from "@/utils/localStorege";
import { getToken } from "@/utils/auth";

export {
  getCookie,
  setCookie,
  hasCookie,
  removeCookie,
  getFromLocalStorege,
  saveInLocalStoreg,
  isInLocalStorge,
  deleteFromLocalStorge,
  getToken,
};
