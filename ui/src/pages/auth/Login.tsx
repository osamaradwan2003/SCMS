import { useAuth } from "@/hooks/auth";
import { useEffect } from "react";

export default function Login() {
  // const { login } = useAuth();
  // useEffect(() => {
  //   login("test", "test");
  // });
  return <>Login</>;
}
